# GitHub Pages Workflow (Vite)
1) Upload `.github/workflows/deploy.yml` to your repo (branch: main).
2) Repo Settings → Pages → Source: GitHub Actions.
3) Optional: Settings → Pages → Custom domain = mojaf-sa.com (then click "Check again").
4) Push to main — build and deploy will run automatically.
